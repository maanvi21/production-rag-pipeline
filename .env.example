GROQ_API_KEY=gsk-your-key-here

# Optional — set these to point at managed cloud services instead of the
# self-hosted docker-compose containers (e.g. when deploying without Docker).
# Leave unset for local `docker compose up`.
QDRANT_URL=
QDRANT_API_KEY=
REDIS_URL=
